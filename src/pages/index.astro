---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 3);

const latestPostUrl = posts.length > 0 ? `/blog/${posts[0].id}/` : '/blog';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<!-- HERO -->
			<section class="mx-auto max-w-3xl px-4 py-20 text-center">
				<h1 class="mb-6 text-4xl font-bold leading-tight text-gray-900 md:text-5xl lg:text-6xl">
					Real AI conversations.<br />Real life decisions.
				</h1>
				<p class="mx-auto mb-10 max-w-2xl text-lg leading-relaxed text-gray-600">
					I'm Carlosante. I document how I use AI to solve everyday situations — from choosing
					paint colors to organizing a closet — with full screenshots, real context, and honest
					results. For people who keep hearing about AI but feel like it's not for them.
				</p>
				<div class="flex flex-col items-center justify-center gap-4 sm:flex-row">
					<a
						href={latestPostUrl}
						class="inline-block rounded-lg bg-blue-600 px-8 py-3 text-lg font-medium text-white no-underline transition-colors hover:bg-blue-700"
					>
						Read the latest story
					</a>
					<a
						href="/about"
						class="inline-block text-lg font-medium text-gray-600 no-underline transition-colors hover:text-gray-900"
					>
						What is this? &rarr;
					</a>
				</div>
			</section>

			<!-- LATEST STORIES -->
			<section class="mx-auto max-w-4xl px-4 py-16">
				<h2 class="mb-10 text-center text-3xl font-bold text-gray-900">Latest Stories</h2>
				{posts.length > 0 ? (
					<div class="grid gap-8 md:grid-cols-3">
						{posts.map((post) => (
							<a href={`/blog/${post.id}/`} class="group block rounded-xl border border-gray-200 p-6 no-underline transition-shadow hover:shadow-lg">
								<p class="mb-2 text-sm text-gray-500">
									<FormattedDate date={post.data.pubDate} />
								</p>
								<h3 class="mb-2 text-lg font-semibold text-gray-900 transition-colors group-hover:text-blue-600">
									{post.data.title}
								</h3>
								<p class="text-sm leading-relaxed text-gray-600">
									{post.data.description}
								</p>
							</a>
						))}
					</div>
				) : (
					<p class="text-center text-gray-500">
						The first story drops this weekend. Check back soon — or reach out at{' '}
						<a href="mailto:contact@lifemeetsai.com">contact@lifemeetsai.com</a> to be notified.
					</p>
				)}
			</section>

			<!-- HOW IT WORKS -->
			<section class="mx-auto max-w-4xl px-4 py-16">
				<h2 class="mb-10 text-center text-3xl font-bold text-gray-900">How this site works</h2>
				<div class="grid gap-8 md:grid-cols-3">
					<div class="rounded-xl border border-gray-100 bg-gray-50 p-6">
						<h3 class="mb-3 text-lg font-bold text-gray-900">Real situations.</h3>
						<p class="text-sm leading-relaxed text-gray-600">
							Not hypothetical use cases. Actual decisions from my life (or readers') —
							paint colors, furniture, gadgets, organization.
						</p>
					</div>
					<div class="rounded-xl border border-gray-100 bg-gray-50 p-6">
						<h3 class="mb-3 text-lg font-bold text-gray-900">Full conversations.</h3>
						<p class="text-sm leading-relaxed text-gray-600">
							Every post includes screenshots of the actual AI conversation.
							You see the prompts, the mistakes, and what came back.
						</p>
					</div>
					<div class="rounded-xl border border-gray-100 bg-gray-50 p-6">
						<h3 class="mb-3 text-lg font-bold text-gray-900">Honest results.</h3>
						<p class="text-sm leading-relaxed text-gray-600">
							I show what worked, what didn't, and what I'd do differently.
							No hype. No shilling. Just honest reporting.
						</p>
					</div>
				</div>
			</section>

			<!-- ABOUT TEASER -->
			<section class="mx-auto max-w-3xl px-4 py-16 text-center">
				<h2 class="mb-6 text-3xl font-bold text-gray-900">About Carlosante</h2>
				<p class="mx-auto mb-6 max-w-2xl text-lg leading-relaxed text-gray-600">
					I'm 27, based in Sweden, and just a regular person who stumbled into realizing
					how useful AI can be when you stop treating it like a chatbot and start treating
					it like a smart friend who's seen everything.
				</p>
				<a
					href="/about"
					class="inline-block text-lg font-medium text-blue-600 no-underline transition-colors hover:text-blue-800"
				>
					Read more &rarr;
				</a>
			</section>
		</main>
		<Footer />
	</body>
</html>
